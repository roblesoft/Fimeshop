<h1 class="display-1">Productos</h1>
<% @products.each do |product| %>
	<%= product.name %><%= link_to 'Edit', edit_product_path( product ) %><%= link_to 'delete', product, method: :delete, data: {confirm: 'sure?' } %>  
<br>
<% end %>
